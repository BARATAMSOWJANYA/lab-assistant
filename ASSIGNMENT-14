#include <stdio.h>
void reverse(int n)
{
    if (n<=0)
        return;
    else
    {
        printf("%d ",n);
        reverse(n-1);
    }
}
int main()
{
    int n;
    printf("Enter n value : \n");
    scanf("%d",&n);
    reverse(n);
    return 0;
}
